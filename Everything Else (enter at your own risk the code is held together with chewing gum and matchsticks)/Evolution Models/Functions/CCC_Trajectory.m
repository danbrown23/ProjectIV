function v = CCC_Trajectory(t,d)
% given a time t, returns the d dimensional vector of the centre of the
% carrying capacity
v = 0*[1 ; zeros(d-1,1)]*t; % moving in the x-direction at constant speed
end
